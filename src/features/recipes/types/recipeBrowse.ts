import type { LucideIcon } from "lucide-react";

export type RecipePreview = {
  detail: string;
  label: string;
  summary: string;
  title: string;
};

export type RecipeBrowsePrinciple = {
  description: string;
  icon: LucideIcon;
  title: string;
};
